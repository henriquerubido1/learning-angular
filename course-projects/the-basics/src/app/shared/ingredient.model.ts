export class Ingredient {
  // this is a simplified way to create a new instance of an object
  // a longer example of this section is recipe.model.ts
  constructor(public name: string, public amount: number) {}
}
